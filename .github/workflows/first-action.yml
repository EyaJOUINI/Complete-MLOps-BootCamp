name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on:
      ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello World from GitHub Actions"
      - name : Print Googbye
        run: echo "Done ;) bye!"
  
